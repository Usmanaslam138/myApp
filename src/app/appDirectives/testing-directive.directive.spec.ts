import { TestingDirectiveDirective } from './testing-directive.directive';

describe('TestingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TestingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
