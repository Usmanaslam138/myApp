import { DropdowndDirective } from './dropdownd.directive';

describe('DropdowndDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdowndDirective();
    expect(directive).toBeTruthy();
  });
});
